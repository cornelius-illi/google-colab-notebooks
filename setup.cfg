[metadata]
name = gemini-podcast-transcription
version = 0.0.0
description = Transcribe audio from links using Gemini

[options]
py_modules = transcribe_from_link
install_requires =
    # keep runtime deps in requirements.txt; list core runtime deps if needed here
